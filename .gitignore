key.json
*.key.json
gke-key.json
.env
__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/
# one-shot setup scripts (contain hard-coded local paths/IDs)
setup-gcp.sh
setup-gcp-retry.sh
deploy-gke.sh
resume-deploy.sh
diagnose.sh
rebuild-and-restart.sh
cleanup.sh
# Week 3 / 4: do NOT commit raw parquets (per README — too large)
week3/data/
week4/data/
*.parquet
# But keep the small lookup parquet in backend/
!week3/backend/zone_hour_avg_fare.parquet
# Week 4 metrics JSON outputs (CI artifacts, not source)
week4/metrics-*.json
week4/drift-findings*.json
# Week 5 / 6: do NOT commit the 50MB SQLite DB (comes from the course repo). Keep
# the small JSON data files so policy/expense tools remain runnable from the repo.
week5/data/techcorp.db
week6/data/techcorp.db
# Root-level submission staging (was used as a one-time scratch dir)
/submission/
# Per-week submission/ folders ARE committed as a record of what was uploaded to Canvas.
# pytest cache
.pytest_cache/
